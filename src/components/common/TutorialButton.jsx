import styled from 'styled-components';
import { Link } from 'react-router-dom';

const TutorialButton = styled(Link)`
  width: auto;
  background: none;
  border: none;
  cursor: pointer;
  padding: 0;
  margin: 0;
`;

export default TutorialButton;
